resource Handle {
    conn: Socket,
    cleanup: close_handle,
}

fn borrow_read(h: Handle) -> String
fn borrow_write(h: Handle, data: String) -> ()

fn use_handle(h: Handle) -> () {
    let data = borrow_read(h)
    borrow_write(h, data)
}
