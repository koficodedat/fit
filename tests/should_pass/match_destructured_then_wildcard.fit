resource Handle { fd: Int, cleanup: close }
fn close(h: move Handle) -> ()

enum Container { HasOne(Handle), Empty, Other }

fn make() -> Container

fn process() -> () {
    match make() {
        HasOne(h) => { close(h) },
        _ => { },
    }
}
