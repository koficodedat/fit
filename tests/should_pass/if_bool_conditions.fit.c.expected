#include <stdio.h>
#include <string.h>
#include <stdlib.h>

typedef int Bool;
typedef int Int;

extern Bool get_flag(void);
extern Int get_count(void);

void branches(Int n) {
  if (1) {
  } else {
  }
  if (get_flag()) {
  } else {
  }
  if ((n > 0)) {
  } else {
  }
  if (((n + 1) == 2)) {
  } else {
  }
  return;
}

